# Binance testnet/read-only placeholders.
# Keep these empty until Phase 1 adds explicit read-only connectors.
BINANCE_API_KEY=
BINANCE_API_SECRET=
BINANCE_SPOT_BASE_URL=https://testnet.binance.vision
BINANCE_USDM_BASE_URL=https://testnet.binancefuture.com

# Database (PostgreSQL)
DATABASE_URL=postgresql://degenking:degenking@localhost:5432/degenking

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json
